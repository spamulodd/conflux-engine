[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
# Minimum supported Rust version (MSRV)
# Current transitive deps (zeroize 1.9+, idna_adapter 1.2+) require Rust 1.85+
min-version = "1.85.0"
