[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
# Minimum supported Rust version (MSRV)
# Current transitive deps (icu 2.2+, idna_adapter 1.2+) require Rust 1.86+
min-version = "1.86.0"
