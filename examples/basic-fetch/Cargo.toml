[package]
name = "basic-fetch"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
conflux-engine = { path = "../.." }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
